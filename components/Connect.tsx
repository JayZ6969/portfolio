import { FC } from 'react';
import Image from 'next/image';

const classes = {
    Container: 'flex items-center h-svh justify-center w-full bg-[#DFFF00]',

}

const Connect: FC = () => {
    return (
        <div className={classes.Container}>

        </div>
    );
}

export default Connect;